"main";_52a‍.e([["Josephus",()=>Josephus]]);_52a‍.w('@std/esm');

class Josephus {

	constructor() {
        //do stuff
    }

	static josephus(n, k) {
		let r;
		if (k < 1) {
			return;
		}
		let circle = [];
		let effectiveCount = n;
		for(let i=0; i < n; ++i) {
			circle.push(i+1);
		}
		console.log(circle);
		let pos = 0;

		while(circle.length > 1) {
			pos = (pos + k)
			if (pos > effectiveCount) {
				console.log(pos, effectiveCount);
				pos = (pos % effectiveCount) + 1;
				effectiveCount = circle.length;
				console.log(pos, effectiveCount);
			} 
			r = circle.splice(pos - 1, 1);
			console.log('removing element ', r);
		}


	}
};

Josephus.josephus(10,3);

console.log('Hello');