"main";_c39‍.e([["Josephus",()=>Josephus]]);_c39‍.w('@std/esm');

class Josephus {

	constructor() {}

	static josephus(n, k) {
		let r;
		if (k < 1) {
			return;
		}
		let circle = [];
		let sequence = [];
		let effectiveCount = n;
		for(let i=0; i < n; ++i) {
			circle.push(i+1);
		}
		let pos = 0;

		while(circle.length > 1) {
			pos = (pos + k)
			if (pos >= circle.length) {
				pos = (pos % circle.length);
			} 
			r = circle.splice(pos - 1, 1);
			pos = pos - 1;
			sequence.push(r);
		}
		sequence.push(circle);
		console.log(sequence);
		return sequence;
	}
};

Josephus.josephus(8, 2);
Josephus.josephus(15, 6);