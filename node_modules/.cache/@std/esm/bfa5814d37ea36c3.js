"main";_37e‍.e([["Josephus",()=>Josephus]]);_37e‍.w('@std/esm');

class Josephus {

	constructor() {
        //do stuff
    }

	static josephus(n, k) {
		let r;
		if (k < 1) {
			return;
		}
		let circle = [];
		let effectiveCount = n;
		for(let i=0; i < n; ++i) {
			circle.push(i+1);
		}
		let pos = 0;

		while(circle.length > 1) {
			pos = (pos + k)
			if (pos >= circle.length) {
				pos = (pos % circle.length);
			} 
			r = circle.splice(pos - 1, 1);
			pos = pos - 1;
			console.log('removing element ', r);
		}

		console.log('The winner is:', circle);

	}
};

Josephus.josephus(28,1);

console.log('Hello');