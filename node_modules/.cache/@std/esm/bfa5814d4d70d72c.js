"main";_4d7‍.e([["Josephus",()=>Josephus]]);_4d7‍.w('@std/esm');

class Josephus {

	constructor() {
        //do stuff
    }

	static josephus(n, k) {
		let r;
		if (k < 1) {
			return;
		}
		let circle = [];
		let effectiveCount = n;
		for(let i=0; i < n; ++i) {
			circle.push(i+1);
		}
		console.log(circle);
		let pos = 0;

		while(circle.length > 1) {
			pos = (pos + k)
			if (pos >= circle.length) {
				console.log(pos, circle.length);
				pos = (pos % circle.length);
				console.log(pos, effectiveCount);
			} 
			r = circle.splice(pos - 1, 1);
			pos = pos - 1;
			console.log('removing element ', r);
		}


	}
};

Josephus.josephus(28,3);

console.log('Hello');